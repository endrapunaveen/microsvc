{
  "title": "createProduct",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "command": {
      "type": "string"
    },
    "payload": {
      "type": "object",
      "properties": {
        "productId": {
          "type": "string"
        },
        "category": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "brand": {
          "type": "string"
        }
      },
      "required": ["productId", "category", "status"]
    }
  },
  "required": ["id", "command", "payload"]
}
